# Core framework
fastapi
uvicorn
python-multipart

# AI & Embedding
ollama
strands-agents

# Vector database
faiss-cpu
numpy
rank-bm25

# Document processing
pypdf
langchain-text-splitters
python-docx     # DOCX extraction
python-pptx     # PPTX extraction
openpyxl        # XLSX extraction
httpx           # URL fetching
trafilatura     # Clean web text extraction (also used for HTML files)
pymupdf         # PDF page rendering (used by OCR fallback)
pytesseract     # Tesseract OCR wrapper (requires: brew/apt install tesseract)
Pillow          # Image manipulation (pytesseract dependency)

# Audio transcription (local Whisper — optional, enables mic button)
faster-whisper  # lightweight CTranslate2-based Whisper inference

# Durable workflows
dbos
sqlalchemy
psycopg2-binary

# Configuration
pydantic-settings>=2.0.0

# Testing (install with: pip install -r requirements.txt)
pytest
pytest-asyncio
